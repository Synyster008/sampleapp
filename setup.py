from setuptools import setup, find_packages

setup(
    name="sampleapp00",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "setuptools>=61",
        "setuptools_scm>=8",
    ],
)
